### Test

Run:

```bash
uvicorn main:app --reload
```

Then open:

```text
http://127.0.0.1:8000/
```

Response:

```json
{
    "message": "Enterprise IT Service Desk API"
}
```

Swagger:

```text
http://127.0.0.1:8000/docs
```

**At this stage, the objective is simply:**
`FastAPI application → IT Service Desk application`


